import React from 'react';
import FontAwesome from 'react-fontawesome';

const DropdownListItem = ({title}) => (
  <p className="dropdown__submenu__item">{title}</p>
);

export default DropdownListItem;

